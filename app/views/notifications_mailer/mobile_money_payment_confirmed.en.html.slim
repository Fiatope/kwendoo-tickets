- if @notification.contribution.present?
  - resource = @notification.contribution
- else
  - resource = @notification.match

|Hello, #{resource.user.display_name}!
br
br
|This confirms your payement to #{link_to(resource.project.name, project_url(resource.project))}. Please keep this email as a receipt of your mobile money payment.This ticket is scanned once, so it is your responsibility to protect it from unauthorized user.
br
br
|Payement Amount:
strong #{number_to_currency resource.value, unit: "RWF", precision: 0}
br
br
|Payement Created at:
strong #{l resource.created_at.to_date, format: :long}
br
br
|Transaction Reference:
strong #{resource.transaction_number}
br
br
|You will be informed of the payment of this event to its beneficiary.
br
br
|If you want to contact #{resource.project.user.name}, send an email to #{mail_to resource.project.user.email}.
br
br
|If you have any other questions, please access our #{link_to('FAQ', "#{Configuration[:base_url]}/faq")}.
br
br
|Kindest regards,
br
|The #{Configuration[:company_name]} Team
